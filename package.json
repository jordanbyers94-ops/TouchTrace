{
  "name": "touchtrace",
  "version": "1.0.0",
  "private": true,
  "description": "TouchTrace — switchboard field scan tool for licensed electricians (AS/NZS 3000 reference). Static app + serverless proxy that keeps the Anthropic API key server-side.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
